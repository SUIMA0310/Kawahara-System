<UserControl x:Class="DesktopApp.Views.InputSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             MinWidth="400">
    <StackPanel Margin="10">
        <StackPanel Margin="10">
            <Label>Server URL</Label>
            <TextBox Text="{Binding ServerURL.Value}"/>
        </StackPanel>
        <StackPanel Margin="10">
            <Label>Presentation ID</Label>
            <TextBox Text="{Binding PresentetionID.Value}"/>
        </StackPanel>
        <StackPanel Margin="10" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="5, 0" 
                    Width="60" 
                    Content="OK" 
                    Command="{Binding OK}"/>
            <Button Margin="5, 0" 
                    Width="60" 
                    Content="Cancel" 
                    Command="{Binding Cancel}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
